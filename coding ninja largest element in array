#include<iostream>
    int largestElement(vector<int>& arr, int n)
    {
        int mn = arr[0];
        for(int i=0;i<n;i++)
        {
            if(mn<arr[i])
            {
                mn = arr[i];
            }
        }
        return mn;
}
